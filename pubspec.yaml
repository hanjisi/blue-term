name: blueterm
description: "A Bluetooth serial terminal."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.0
  flutter_blue_plus: ^2.1.0
  shared_preferences: ^2.5.4
  path_provider: ^2.1.5
  enough_convert: ^1.6.0
  uuid: ^4.5.2
  intl: ^0.20.2
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true